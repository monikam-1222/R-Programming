# Experiment 19: Create a list from vectors, matrices and functions
vec <- c(1, 2, 3)
mat <- matrix(1:4, 2, 2)
fun <- function(x) x^2
lst <- list(Vector = vec, Matrix = mat, Function = fun)
print(lst)